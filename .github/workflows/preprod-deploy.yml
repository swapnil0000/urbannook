name: Pre-Prod Deploy

on:
  push:
    branches:
      - pre-prod

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Node setup
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # 3️⃣ Build client
      - name: Build Client
        working-directory: client
        run: |
          npm ci
          npm run build

      # 4️⃣ Install server deps
      - name: Install Server Dependencies
        working-directory: server
        run: npm ci

      # 5️⃣ Setup SSH (NON-FAILING)
      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.PREPROD_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.PREPROD_HOST }} >> ~/.ssh/known_hosts || true

      # 6️⃣ Check EC2 availability
      - name: Check Pre-Prod EC2 availability
        run: |
          echo "Checking Pre-Prod EC2..."
          if ssh -o ConnectTimeout=5 ubuntu@${{ secrets.PREPROD_HOST }} "echo EC2 is running" >/dev/null 2>&1; then
            echo "EC2_AVAILABLE=true" >> $GITHUB_ENV
          else
            echo "EC2_AVAILABLE=false" >> $GITHUB_ENV
            echo "Pre-Prod EC2 is stopped. Deployment skipped."
          fi

      # 7️⃣ Upload client (ONLY IF EC2 UP)
      - name: Upload Client
        if: env.EC2_AVAILABLE == 'true'
        run: |
          rsync -avz --delete \
          client/dist/ \
          ubuntu@${{ secrets.PREPROD_HOST }}:/home/ubuntu/urbannook-pre-prod/client/dist/

      # 8️⃣ Upload server
      - name: Upload Server
        if: env.EC2_AVAILABLE == 'true'
        run: |
          rsync -avz \
          server/ \
          ubuntu@${{ secrets.PREPROD_HOST }}:/home/ubuntu/urbannook-pre-prod/server/

      # 9️⃣ Restart PM2 (NO EC2 START)
      - name: Restart Server
        if: env.EC2_AVAILABLE == 'true'
        run: |
          ssh ubuntu@${{ secrets.PREPROD_HOST }} << 'EOF'
            cd /home/ubuntu/urbannook-pre-prod/server
            pm2 restart urbannook-preprod || pm2 start src/server.js --name urbannook-preprod
            pm2 save
          EOF
